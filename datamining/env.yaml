# GCP Cloud Run Job Environment Template
# You can deploy using:
# gcloud run jobs deploy email-miner --source . --env-vars-file env.yaml

GEMINI_API_KEY: "AIzaSyArqGgQpC-RIzit-ATWX9JXs_c-zB0CYjQ"
GOOGLE_PROJECT_ID: "ai-personal-assistant-490217"
GCS_MEMORIES_BUCKET: "ai-personal-assistant"

# The job will use the tokens.json file (if provided) to authenticate
# You might want to use a service account for better security in a real production environment
# But for now, ensuring tokens.json is copied into the container is the simplest route.
